## Build (Windows)
1. Open a terminal (Git Bash or PowerShell) in this folder.  
2. Run:
   ```bash
   cmake -B build
   cmake --build build
3. Open: build/SuperMario.sln
    then press Ctrl + Shift + B in Visual Studio to build and run.